interface ZonesStepProps { data?: unknown; onNext?: () => void; onBack?: () => void; }
export function ZonesStep({ data: _data, onNext: _onNext, onBack: _onBack }: ZonesStepProps) {
  return (
    <div>
      <h2>Paso de Zonas</h2>
      {/* Implementación pendiente */}
    </div>
  );
}
